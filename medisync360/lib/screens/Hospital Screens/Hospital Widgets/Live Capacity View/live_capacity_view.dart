import 'package:flutter/material.dart';

class LiveCapacityView extends StatefulWidget {
  const LiveCapacityView({super.key});

  @override
  State<LiveCapacityView> createState() => LiveCapacityViewState();
}

class LiveCapacityViewState extends State<LiveCapacityView> {
  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child : Text("WElcome to the ")
    );
  }
}